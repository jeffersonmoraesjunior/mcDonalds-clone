import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
   boxTop: {
      marginTop: 25,
      marginBottom: 10,
      alignItems: 'center'
   },
   image: {
      width: 35,
      height: 30
   }
});

export default styles;
